{
  "name": "zipkiss",
  "version": "0.1.0",
  "description": "Extremely simple .zip file creation with no dependencies",
  "main": "index.js",
  "scripts": {
    "check": "tsc --noEmit",
    "buildCjs": "esbuild index.ts --bundle --format=cjs --minify --line-limit=80 --outfile=index.js",
    "buildEsm": "esbuild index.ts --bundle --format=esm --minify --line-limit=80 --outfile=index.mjs",
    "build": "npm run buildCjs && npm run buildEsm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "zip",
    "deflate",
    "compressionstream"
  ],
  "author": "George MacKerron",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.1",
    "typescript": "^5.3.3"
  }
}
